<UserControl x:Class="CatálogoDeProductos.Views.GraficosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:vm="clr-namespace:CatálogoDeProductos.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CatálogoDeProductos.Views"
             xmlns:resources="clr-namespace:CatálogoDeProductos.Resources"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="7*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="10" Text="{x:Static resources:Resources.txtCantidadDeProductosPorCategoria}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="22" Foreground="{DynamicResource Foreground_Primario}" FontWeight="Bold"/>
            <lvc:PieChart Grid.Row="1" Series="{Binding ProductosPorCategoriaGrafico}" LegendPosition="Bottom"/>
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="7*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="10" Text="{x:Static resources:Resources.txtProductosPorRangoDePrecios}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="22" Foreground="{DynamicResource Foreground_Primario}" FontWeight="Bold"/>
            <lvc:PieChart Grid.Row="1" Series="{Binding ProductosPorRangoDePreciosGrafico}" LegendPosition="Bottom"/>
        </Grid>
    </Grid>
</UserControl>
