<UserControl x:Class="CatálogoDeProductos.Views.ConfiguracionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:CatálogoDeProductos.ViewModels"
             xmlns:resources="clr-namespace:CatálogoDeProductos.Resources" 
             d:DataContext="{d:DesignInstance Type=vm:ConfiguracionViewModel}"
             mc:Ignorable="d"
             Height="500" Width="525">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/Templates/RadioButtonTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="75"/>
                <RowDefinition Height="75"/>
                <RowDefinition Height="75"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{x:Static resources:Resources.txtTema}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource Foreground_Primario}"/>
            <RadioButton x:Name="btnClaro" Grid.Row="2" Margin="10" GroupName="Tema" Content="{x:Static resources:Resources.btnClaro}" FontSize="18" Foreground="{DynamicResource Foreground_Primario}" Command="{Binding CambiarTemaClaroCommand}"/>
            <RadioButton x:Name="btnOscuro" Grid.Row="3" Margin="10" GroupName="Tema" Content="{x:Static resources:Resources.btnOscuro}" FontSize="18" Foreground="{DynamicResource Foreground_Primario}" Command="{Binding CambiarTemaOscuroCommand}"/>
        </Grid>

        <Grid Grid.Column="2" >
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="75"/>
                <RowDefinition Height="75"/>
                <RowDefinition Height="75"/>
                <RowDefinition Height="75"/>
                <RowDefinition Height="75"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="1" FontWeight="Bold" Text="{x:Static resources:Resources.txtLenguaje}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" Foreground="{DynamicResource Foreground_Primario}"/>
            <RadioButton x:Name="btnEsp" Grid.Row="2" GroupName="Idioma" Margin="10" Content="{x:Static resources:Resources.btnEsp}" FontSize="22" Foreground="{DynamicResource Foreground_Primario}" Command="{Binding CambiarEspCommand}"/>
            <RadioButton x:Name="btnEng" Grid.Row="3" GroupName="Idioma" Margin="10" Content="{x:Static resources:Resources.btnEng}" FontSize="22" Foreground="{DynamicResource Foreground_Primario}" Command="{Binding CambiarIngCommand}"/>
            <RadioButton x:Name="btnFra" Grid.Row="4" GroupName="Idioma" Margin="10" Content="{x:Static resources:Resources.btnFra}" FontSize="22" Foreground="{DynamicResource Foreground_Primario}" Command="{Binding CambiarFraCommand}"/>
            <RadioButton x:Name="btnAle" Grid.Row="5" GroupName="Idioma" Margin="10" Content="{x:Static resources:Resources.btnAle}" FontSize="22" Foreground="{DynamicResource Foreground_Primario}" Command="{Binding CambiarAleCommand}"/>
        </Grid>
    </Grid>
</UserControl>
